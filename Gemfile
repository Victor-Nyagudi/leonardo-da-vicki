# frozen_string_literal: true

source "https://rubygems.org"

# gem "rails"

# Docs recommend adding the commented line below, but GitHub Pages recommends the other with "~> 232".
# Given the last commit in the repo came in 2019, I'll go with the Github Pages docs instructions.
# gem "github-pages", group: :jekyll_plugins
gem "github-pages", "~> 232", group: :jekyll_plugins